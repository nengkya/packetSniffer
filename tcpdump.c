#include "stdio.h"
#include <stdlib.h>

int main() {
    char * errorBuffer = (char *)malloc(PCAP_ERR_:qsizeof(char *));
    char * device = pcap_lookupdev(errorBuffer);
    fprintf(stderr, "%s",errorBuffer);















}
